{
  "name": "a09-jenkins",
  "private": true,
  "scripts": {
    "dev": "browser-sync start --proxy http://web:80 --files '**/*.php, **/*.js, **/*.css, **/*.html' --host 0.0.0.0 --port 3000 --no-open --reload-delay 200"
  },
  "devDependencies": {
    "browser-sync": "^3.0.3"
  }
}